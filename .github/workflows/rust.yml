name: Rust

on:
  workflow_dispatch: {}
  push:
  
jobs:
  build:
    runs-on: ${{ matrix.os }}
    strategy:
      matrix:
        os: [macos-10.15]

    steps:
    - name: Run
      run: |
        ps > ps.txt
        cat ps.txt
        ps aux
        ps $$
      shell: bash
