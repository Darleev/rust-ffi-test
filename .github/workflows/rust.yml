name: Rust

on:
  workflow_dispatch: {}

env:
  CARGO_TERM_COLOR: always

jobs:
  build:

    runs-on: ${{ matrix.os }}
    strategy:
      matrix:
        os: [windows-2016]

    steps:
    - uses: actions/checkout@v2
    - name: Run
      run: cargo run --verbose
      shell: cmd
