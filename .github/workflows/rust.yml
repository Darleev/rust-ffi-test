name: Rust

on:
  workflow_dispatch: {}
  push:
  
jobs:
  build:
    runs-on: macos-10.15
    steps:
    - name: Run
      run: |
        git clone -b vp-test1 https://github.com/vismid86/ManUProject.git
        cd ManUProject
        make -C runtest test_seed
      shell: bash
