name: Rust

on:
  workflow_dispatch: {}
  push:
  
jobs:
  build:
    runs-on: macos-10.15

    steps:
    - name: Run
      run: |
        sleep 120 &
        tail -fn0 /dev/null &
        sudo strace ps
      shell: bash
